# CLAUDE.md

이 저장소에서 작업할 때 Claude Code가 따라야 할 지침.

## 프로젝트

**물리 퍼즐 + 자동 검증 솔버** — (주)링크즈 × 디벨로켓 협약 프로젝트.
2D 물리 퍼즐 게임(Android 세로 모드, 한 손 조작)과 **레벨을 자동으로 검증하는 솔버**를 함께 만든다.
플레이어는 선을 그어 물체를 만들고 공을 목표까지 굴려 보낸다. 솔버는 만든 레벨을 스스로 수없이 시도해
"풀리는가 / 얼마나 어려운가"를 판정한다.

## 기술 스택

| 항목 | 값 |
|---|---|
| 엔진 | Unity 6000.3.13f1 (Unity 6 LTS 계열) |
| 렌더 | URP (Mobile) 17.3.0 |
| 입력 | Input System 1.19.0 (신형) |
| 테스트 | Unity Test Framework 1.6.0 |
| 빌드 | Android APK, IL2CPP + ARM64, Target API 35, 최소 API 26 |
| 물리 | Physics2D (`PhysicsScene2D.Simulate` 수동 호출) |

## 규칙
### 코딩 전에 생각하라
추측하지 마라. 혼란스러운 부분을 숨기지 마라. 트레이드오프를 드러내라.
구현하기 전에 가정을 명시적으로 밝혀라. 확실하지 않으면 질문하라.
여러 해석이 가능하다면 조용히 하나를 고르지 말고, 그 해석들을 제시하라.
더 단순한 접근법이 있다면 말하라. 필요할 때에는 반대 의견을 내라. 
불명확한 점이 있으면 멈춰라. 무엇이 혼란스러운지 말하고 질문하라.

### 단순함을 우선하라
문제 해결을 위한 최소한의 코드만 작성하라. 추측에 기반한 코드는 쓰지 마라.
요구받지 않은 기능을 추가하지 마라.
한 번만 쓰이는 코드에 추상화를 만들지 마라.
요청받지 않은 “유연성”이나 “설정 가능성”을 추가하지 마라.
발생할 수 없는 상황을 위한 에러 처리를 만들지 마라.
스스로에게 물어라. “시니어 엔지니어가 이걸 보고 과하게 복잡하다고 말할까?” 그렇다면 단순화하라.

### 필요한 부분만 정밀하게 변경하기
반드시 수정해야 하는 부분만 건드려라. 정리는 네가 만든 문제에 대해서만 하라.

기존 코드를 수정할 때:
주변 코드, 주석, 포맷팅을 “개선”하지 마라.
망가지지 않은 코드를 리팩토링하지 마라.
네 방식과 다르더라도 기존 스타일을 따르라.
관련 없는 죽은 코드를 발견하면 언급만 하고, 삭제하지 마라.

네 변경으로 사용되지 않는 코드가 생겼을 때:
네 변경 때문에 더 이상 사용되지 않게 된 import, 변수, 함수는 제거하라.
기존에 이미 있던 죽은 코드는 요청받지 않았다면 제거하지 마라.

판단 기준:
변경된 모든 줄은 사용자의 요청과 직접 연결되어 있어야 한다.

### 목표 중심 실행
성공 기준을 정의하라. 검증될 때까지 반복하라.
작업을 검증 가능한 목표로 바꿔라:
"검증 추가" → "잘못된 입력에 대한 테스트를 작성한 다음, 그것들이 통과하게 만들기"
"버그 수정" → "그것을 재현하는 테스트를 작성한 다음, 그것이 통과하게 만들기"
"X 리팩터링" → "전후로 테스트가 통과하는지 확인하기"

여러 단계 작업의 경우, 간단한 계획을 제시하라:

1. [단계] → 검증: [확인]
2. [단계] → 검증: [확인]
3. [단계] → 검증: [확인]

강한 성공 기준은 네가 독립적으로 반복할 수 있게 해준다.
약한 기준("작동하게 만들기")은 계속된 명확화가 필요하다.


## 주석 작성

**3줄 내외를 권장한다.** 한 줄은 45자 미만으로 끊는다.

- **없어도 되면 쓰지 않는다.** 코드만 봐도 아는 사실을 옮겨 적지 않는다
- **무엇이 아니라 왜를 쓴다.** 특히 결정론 제약처럼 코드에 드러나지 않는 이유
- 필드·메서드 주석은 그 대상의 **역할**을 먼저 적는다. 성질이나 사용법은 그다음이다
- 작성 배경·히스토리·다른 문서 참조는 넣지 않는다

## Git 작업 규칙 (에이전트 금지 — 예외 없음)

**에이전트는 git 작업을 직접 하지 않는다. 전부 사용자에게 위임한다.**
`commit` · `push` · PR 생성 · `merge` · 태그 · 브랜치 이동/삭제 등 저장소 상태나 원격을 바꾸는 명령을
에이전트가 실행하는 일은 **발생해서는 안 된다**. `gh pr create` 도 마찬가지다.
읽기 전용 조회(`git log` · `git diff` · `git status` · `git branch --list`)만 허용된다.

사용자가 커밋·푸시·머지·PR 을 "해달라"고 하면, 그것은 **그 작업에 들어갈 "메시지"만 작성해 달라**는 뜻이다.
결과물은 항상 **복사할 수 있는 형태**(코드 펜스)로 채팅에 제공하고, 실행은 사용자가 한다.

### PR 요청을 받았을 때

1. 대상 브랜치는 **`Develop`** — 없으면 `master` 로 잡는다
2. 현재 브랜치에서 대상 브랜치로의 **변경 내역만** 모은다
3. **자기 브랜치에서 직접 커밋/푸시한 작업만 포함한다.** 도중에 다른 브랜치에서 머지해 들어온 커밋은 제외한다
   (`git log --no-merges --first-parent Develop..HEAD` 로 자기 작업만 추린다)
4. `.github/pull_request_template.md` 를 그대로 가져와 내용을 채운다.
   공통 + 역할별(화면·레벨·코어) 블록이며 **해당 없는 블록은 지운다**
5. 채운 결과 전체를 **하나의 복사 가능한 블록**으로 채팅에 출력한다. PR 은 사용자가 직접 연다
