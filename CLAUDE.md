# CLAUDE.md

이 저장소에서 작업할 때 Claude Code가 따라야 할 지침.

## 프로젝트

**물리 퍼즐 + 자동 검증 솔버** — (주)링크즈 × 디벨로켓 협약 프로젝트.
2D 물리 퍼즐 게임(Android 세로 모드, 한 손 조작)과 **레벨을 자동으로 검증하는 솔버**를 함께 만든다.
플레이어는 선을 그어 물체를 만들고 공을 목표까지 굴려 보낸다. 솔버는 만든 레벨을 스스로 수없이 시도해
"풀리는가 / 얼마나 어려운가"를 판정한다.

핵심 문서 (작업 전 반드시 참고):
- [Docs/솔버_설계서.md](Docs/솔버_설계서.md) — 결정론 코어·솔버·프리미티브·리플레이 설계. **코어 코드의 단일 근거**
- [Docs/미션 가이드_링크즈_물리 퍼즐 솔버.pdf](Docs/미션%20가이드_링크즈_물리%20퍼즐%20솔버.pdf) — 발주 요구사항 원문
- [Docs/Milestones/](Docs/Milestones/) — 마일스톤 정의와 완료 조건

## 기술 스택

| 항목 | 값 |
|---|---|
| 엔진 | Unity 6000.3.13f1 (Unity 6 LTS 계열) |
| 렌더 | URP (Mobile) 17.3.0 |
| 입력 | Input System 1.19.0 (신형) |
| 테스트 | Unity Test Framework 1.6.0 |
| 빌드 | Android APK, IL2CPP + ARM64, Target API 35, 최소 API 26 |
| 물리 | Physics2D (`PhysicsScene2D.Simulate` 수동 호출) |

## 현재 상태

거의 빈 Unity 프로젝트다. `Assets/_Project/Scenes/Main.unity` 외에 스크립트가 없다.
지금 브랜치는 `ADR/Core`, 진행 중인 작업은 **마일스톤 1 (결정론 코어)**.

## 결정론 규칙 (최우선 — 위반하는 코드를 작성하지 말 것)

솔버 검증의 정당성이 전부 여기에 달려 있다. 시뮬레이션에 관여하는 코드에서 아래는 **금지**다.

- `Time.time`, `Time.deltaTime`, `Time.fixedDeltaTime`, `Time.timeScale` 사용 금지
- `FixedUpdate` 사용 금지 — 호출 시점·횟수를 Unity가 관리해 통제 불가
- 코루틴 `WaitForSeconds` 등 시간 기반 대기 금지 — 모든 시뮬 로직은 **스텝 카운트 기반**
- `UnityEngine.Random` 접근 금지 — 주입된 `System.Random(seed)` 단일 인스턴스만 사용
- `dt` 변경 금지. `FixedDt = 1/60` 고정. 배속은 dt를 키우는 게 아니라 **초당 Step 호출 횟수를 늘리는 것**
- 솔버 월드에서 오브젝트 풀링 금지 — 풀 반납 순서가 다음 시도의 생성 순서를 오염시킨다. 시도마다 파괴·재생성
- Physics2D 잡 옵션(스텝 내부 멀티스레딩) 사용 금지 — 부동소수점 합산 순서 변동 위험
- 스레드 병렬화 금지 — Unity API는 메인 스레드 전용. 병렬화는 **프로세스 단위(레벨 간)로만**
- 오브젝트 생성·등록 순서 고정: 장치는 레벨 데이터 순서, 스트로크는 리스트 순서 (Box2D는 바디 등록 순서에 민감)
- 되돌리기/초기화/재시도는 상태 되감기가 아니라 **월드 전파괴 → 재구축**

물리 전진 경로는 프로젝트에 **단 하나** — `SimWorld.Step()` 뿐이다. 다른 곳에서 물리를 진행시키지 말 것.

## 코어 아키텍처

```
[순수 C# — 프레임/렌더링 무지]
SimWorld        한 판의 시뮬 상태 전체. Step()이 유일한 시간 전진 수단
WorldBuilder    LevelData + Solution → 격리 씬에 월드 구축 (등록 순서 고정)
Judge           클리어/실패/정지 판정 + MinGoalDist 계측
WorldHasher     전 바디 상태 순서 해시 (결정론 테스트·디싱크 디버깅)

[MonoBehaviour — 얇은 드라이버]
GameSimDriver   게임 플레이용. 누적기 패턴으로 실시간 전진 (배속·일시정지)
SolverRunner    솔버용. 프레임 예산 또는 무제한 전진
```

- 격리 물리 씬 `SceneManager.CreateScene(..., LocalPhysicsMode.Physics2D)` 사용. **게임 플레이도 같은 격리 씬** — 게임과 솔버의 실행 환경을 통일해야 "솔버 통과 = 유저도 클리어 가능"이 성립한다
- 계약 타입 + 시뮬 실행부는 **별도 asmdef**로 분리하고 UI·렌더링 참조를 차단한다. "코어는 프레임을 모른다"를 컴파일 에러로 강제
- 상태 머신은 2상태뿐: **편집 중(월드 없음) ↔ 시뮬 중(월드 있음)**. 그리기는 시뮬 시작 전에만 가능하고, 일시정지는 관찰 전용(그리기 잠금)

`Step()`의 순서는 박제된 계약이다: **로직 → 물리 → 판정 → 스텝 증가**.

## 팀 인터페이스 계약

아래 타입은 팀 공유 계약이다. 변경하려면 반드시 사용자에게 먼저 확인할 것.

| 계약 | 당사자 | 내용 |
|---|---|---|
| `Stroke` / `Solution` / `PivotJoint` | 본인 ↔ B | 솔버 출력이자 잉크 파이프라인 출력, 리플레이 본체 |
| `SimResult` + `Run(level, sol, seed, maxSteps)` | 본인 (코어 소유) | 조기 종료·MinGoalDist 계측 포함 |
| `LevelData` JSON | C ↔ 전원 | 앵커 생성 원료(시작점·목표·별·장치 위치) + 잉크 제한 |
| `IStepLogic` | 본인 ↔ B | 장치 6종의 결정론 강제 계약 (step + 주입 rng만 제공) |
| 리플레이 스키마 | 본인 ↔ D | 자동 저장 시스템과 공유 |

역할: **본인** = 결정론 코어 + 솔버 + 프리미티브 + 리플레이 (크리티컬 패스) /
**B** = 잉크 파이프라인 + 장치 6종 / **C** = LevelData·에디터·레벨 디자인 / **D** = UI·튜토리얼·자동 저장·빌드.

## 설계상 확정 사항 (재논의 대상 아님)

이미 팀이 확정한 결정이다. 코드가 이를 벗어나면 잘못된 것은 코드다.

1. 그리기는 시뮬 **시작 전에만** 가능 → 솔루션에 시간 축이 없다
2. 솔루션 = 시간 축 없는 **정적 스트로크 집합**
3. 솔버와 게임은 **동일한 코드 경로**를 사용
4. 솔버 판정은 **하한 증명** — 거짓 양성(안 풀리는데 풀린다)은 구조적으로 없다
5. **역방향 변환(Stroke → θ)은 범위 밖**. 프리미티브는 `Generate()` 단방향뿐
6. 리플레이는 **물리에 투입된 최종 스트로크**를 저장 (θ나 단순화 전 원본이 아님)
7. 렌더링도 **물리와 동일한 단순화 스트로크**를 사용 (`visualPoints` 같은 건 없다). 크레용 감성은 셰이더에서
8. 솔버 시뮬 시간 상한 **30초 = 1,800스텝**
9. 프리미티브 초기 어휘 **7종** (경사면·발판·벽·상자·그릇·지렛대·바퀴)

## 구현 순서 (리스크 순 — 앞 단계 미완 시 다음 단계 착수 금지)

1. **결정론 코어 + 계약 확정** ← 지금 여기. 결정론 자동 테스트 초록불이 완료 조건. 이후 코어 동결
2. 헤드리스 호스트 + 조기 종료 → 즉시 벤치마크 실측
3. 프리미티브 + 무작위 탐색으로 엔드투엔드 (팀 첫 데모, 6주차)
4. CEM 업그레이드
5. 난이도 2지표 + 분류
6. 다양성 클러스터링 + 리포트 export

## 코드 컨벤션

- 파일 배치는 `Assets/_Project/` 아래. 코어는 `Assets/_Project/Scripts/Core/`, 솔버는 `.../Solver/`
- asmdef로 어셈블리를 나누고, 코어 asmdef는 UI·렌더링 어셈블리를 **참조하지 않는다**
- 공개 API에는 XML 문서 주석보다 **왜 이렇게 했는지**를 짧게 남긴다 (결정론 제약은 코드만 봐선 안 보인다)
- 튜닝 가능한 수치(예산 회차, 난이도 기준값, 노이즈 σ)는 하드코딩하지 말고 **설정값/const로 한곳에** 모은다. 설계서의 수치는 전부 벤치마크 전 **예시 초기값**이다
- 테스트는 Unity Test Framework EditMode. 결정론 테스트는 회귀 감시용으로 항상 유지

## 작업 방식

- 코어(SimWorld/WorldBuilder/Judge/계약 타입)를 수정하면 **결정론 테스트를 반드시 다시 돌린다**
- 설계서와 어긋나는 구현이 필요해 보이면 임의로 바꾸지 말고 먼저 알린다
- 브랜치 전략과 코드 리뷰 이력이 평가 항목이다. 커밋은 사용자가 요청할 때만 수행한다
- 문서는 한국어로 작성한다
